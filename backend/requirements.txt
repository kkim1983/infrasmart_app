# ── 웹 프레임워크 ─────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# ── 데이터베이스 ──────────────────────────────
sqlalchemy==2.0.28
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# ── 인증 ─────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# ── 설정 / 검증 ───────────────────────────────
pydantic==2.6.3
pydantic-settings==2.2.1

# ── 캐시 ─────────────────────────────────────
redis==5.0.2

# ── 파일 처리 ─────────────────────────────────
openpyxl==3.1.2
Pillow==10.2.0
python-magic==0.4.27
aiofiles==23.2.1

# ── HTTP 클라이언트 (Webhook 발송) ─────────────
httpx==0.27.0

# ── 유틸 ─────────────────────────────────────
python-dateutil==2.9.0
pytz==2024.1

# ── Phase 2: CAD 변환 ─────────────────────────
ezdxf==1.3.4

# ── Phase 2: OCR / PDF 파싱 ──────────────────
pdfplumber==0.11.0        # PDF 텍스트/테이블 추출 (외관조사망도 파싱)
# paddleocr==2.7.3    # 설치 시 주석 해제
# paddlepaddle==2.6.1

# ── Phase 2: AI (Whisper, 손상분석) ───────────
openai==1.12.0

# ── Phase 2: 암호화 (감사로그, 디지털서명) ──────
cryptography==42.0.4

# ── 개발/테스트 ───────────────────────────────
pytest==8.0.2
pytest-asyncio==0.23.5
httpx==0.27.0
